{
  "buildCommand": "cd frontend && npm install && npm run build:ci",
  "outputDirectory": "frontend/build",
  "installCommand": "npm install --prefix frontend",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
